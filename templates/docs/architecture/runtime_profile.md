# 运行时特征档案

> 版本：1.0 | 日期：{YYYY-MM-DD}
>
> 本文档是工程的"运行时静态描述"——AI 无法直接感知运行时数据，但通过本档案可以建立"运行时特征心智模型"。

---

## 1. 流量模式

- **峰值时段**：`{HH:MM - HH:MM}`
- **峰谷比**：`{N}` : 1
- **突发特征**：`{如 促销 / 活动期 N 倍尖峰；早高峰 / 整点流量等}`
- **周期性**：`{日级 / 周级 / 月级}`

---

## 2. 数据规模

| 数据 | 当前量 | 月增长 | 热数据比例 |
|------|--------|--------|-----------|
| `{example_table-A}` | `{N}` 行 | `{M}` 行 / 月 | `{P}` % |
| `{ns}:info:*` | `{N}` keys | `{M}` keys / 月 | `{P}` % |

---

## 3. 资源消耗基线

| 资源 | 日常值 | 峰值 | 上限 |
|------|--------|------|------|
| CPU | `{N}` core | `{M}` core | `{Max}` core |
| Memory | `{N}` GB | `{M}` GB | `{Max}` GB |
| Redis 连接 | `{N}` | `{M}` | `{Max}` |
| MySQL 连接 | `{N}` | `{M}` | `{Max}` |
| Kafka 消费 lag | `{N}` msg | `{M}` msg | `{Max}` msg |

---

## 4. 关键超时链

```
{Upstream} ──┬─→ 本服务 ──┬─→ {Down-A}
   {T-up}ms  │  {T-self}ms │   {T-down}ms
             │             │
             └─────────────┴─→ {Down-B}
                              {T-down-B}ms

关系约束（与 performance_contract.md §6.2 链路超时协调表一致）：
  {T-self} + max({T-down}, {T-down-B}) < {T-up} * 0.8
```

---

## 5. 更新触发器

| 触发条件 | 更新动作 | 责任方 | AI 可执行性 |
| --- | --- | --- | --- |
| 每季度技术评审 | 全文档对照真实运行数据校对 | 项目负责人 | 🔴 需人工（AI 无运行时数据访问） |
| 流量翻倍 / SLA 调整 | §1 流量模式 + §3 资源消耗基线同步 | 项目负责人 | 🔴 需人工（决策类） |
| 引入新依赖（DB / Cache / MQ）— git diff 可识别 | §3 资源消耗基线追加；§4 关键超时链评估 | 接入方开发者 + AI 辅助 | 🟢 AI 可在 B1 增量同步中提示 |
| Observability 数据呈现 P99 / QPS 长期偏离 | 静态档案与现实脱节 → 触发 §1/§3 重写 | 人工（接入 APM 后可逐步自动化） | 🔴 需人工 |

> 责任划分原则：AI 仅承担"从 git diff 能识别的迹象"对应的同步动作；运行时数据驱动的更新一律标 🔴 需人工。

---

## 6. 与 BUILD_STATUS.md 的关系

`runtime_profile.md` **不**进入 BUILD_STATUS 的"代码模块状态"轨道，独立放在 `BUILD_STATUS.md §12 运行时基线区域`，记录：

- 上次校对日期
- 数据来源（APM / Prometheus / 人工估算）
- 与上次基线的偏离百分比

---

## 7. 与其他文档的关系

| 内容 | runtime_profile.md | performance_contract.md | observability.md |
|------|-------------------|------------------------|------------------|
| 流量模式（静态描述） | ✅ 真相源 | 引用 | 不写 |
| SLA 目标值 | 引用 | ✅ 真相源 | 引用（告警阈值） |
| 资源消耗基线（实测） | ✅ 真相源 | 不写 | 引用（告警条件） |
| 关键超时链 | ✅ 真相源（与 perf_contract §6.2 双向对齐） | ✅ 真相源（与本档案双向对齐） | 不写 |
| Metric 实测数据 | 引用 / 提示 | 不写 | ✅ 真相源 |


---

> 🧩 **AIWeave 骨架 · 作者 XuRuibo** <hustxurb@163.com> · Apache-2.0 · 模板文件，复制到工程后按业务语义填充
